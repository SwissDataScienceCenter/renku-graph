apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-service.fullname" . }}
  labels:
    app: {{ template "webhook-service.name" . }}
    chart: {{ template "webhook-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webhook-service.name" . }}
    release: {{ .Release.Name }}
