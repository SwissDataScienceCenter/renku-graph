apiVersion: v1
kind: Service
metadata:
  name: {{ include "graph.fullname" . }}-graph-service
  labels:
    app: {{ template "graph.name" . }}
    chart: {{ template "graph.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.graphService.service.type }}
  ports:
    - port: {{ .Values.graphService.service.port }}
      targetPort: http-graph-sv
      protocol: TCP
      name: http
  selector:
    app: {{ template "graph.name" . }}
    release: {{ .Release.Name }}
