apiVersion: v1
kind: Service
metadata:
  name: {{ include "graph.fullname" . }}-triples-generator
  labels:
    app: {{ template "graph.name" . }}
    chart: {{ template "graph.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.triplesGenerator.service.type }}
  ports:
    - port: {{ .Values.triplesGenerator.service.port }}
      targetPort: http1
      protocol: TCP
      name: http
  selector:
    app: {{ template "graph.name" . }}
    release: {{ .Release.Name }}
