include "service-commons.base.conf"

play.http.filters = "ch.datascience.webhookservice.Filters"

pidfile.path = "/dev/null"
play.http.context="/"

play.modules.enabled += "ch.datascience.webhookservice.ServiceModule"

queue {
  buffer-size = 10
  buffer-size = ${?QUEUE_BUFFER_SIZE}
  triples-finder-threads = 20
  triples-finder-threads = ${?TRIPLES_FINDER_THREADS}
  fuseki-upload-threads = 20
  fuseki-upload-threads = ${?FUSEKI_UPLOAD_THREADS}
}

services {
  fuseki {
    url = "http://localhost:3030"
    url = ${?JENA_BASE_URL}
    dataset-name = "renku"
    dataset-name = ${?JENA_DATASET_NAME}
    dataset-type = "mem"
    dataset-type = ${?JENA_DATASET_TYPE}
    username = "admin"
    password = ${?JENA_ADMIN_PASSWORD}
  }
}