services {

  fuseki {
    admin {
      password = "admin"
    }
    renku {
      username = "renku"
      password = "renku"
    }

    url = "http://fuseki"

    dataset-name = "renkutest"
  }

  renku {
    url = "http://dev.renku.ch"
  }

  token-repository {
    url = "http://token-repo"
  }

  gitlab {
    url = "http://gitlab"
  }
}
